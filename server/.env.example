# Server Configuration
SERVER_ADDR=0.0.0.0:8080

# Database Configuration
DATABASE_URL=postgres://user:password@localhost:5432/to_do_list

# Database Connection Pool
DB_POOL_MAX_SIZE=10

# Environment
ENVIRONMENT=development

# Logging
LOG_LEVEL=info

# Google OAuth 2.0 Configuration
# Register your application at https://console.cloud.google.com/apis/credentials
# and set the authorized redirect URI to match your server
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8080/auth/callback

# JWT Configuration
# Generate a strong secret key for production (at least 32 characters)
# You can generate one with: openssl rand -hex 32
JWT_SECRET=your-strong-jwt-secret-key-change-in-production
JWT_EXPIRES_IN_HOURS=24
# Optional: Set JWT issuer and audience for additional security
# JWT_ISSUER=to-do-list-server
# JWT_AUDIENCE=to-do-list-client
